# Dashboard

The Power BI (`.pbix`) or Tableau (`.twbx`) file combining segment
breakdown, churn risk by segment, and the sales forecast lives here
(Phase 6), once the upstream tables/exports it reads from exist.
